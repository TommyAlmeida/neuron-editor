export enum GeometryType {
    Box = "box",
    Sphere = "sphere",
    Plane = "plane",
    Torus = "torus",
    Cylinder = "cylinder",
    Custom = "custom"
}

export type Vector3d = [number, number, number]